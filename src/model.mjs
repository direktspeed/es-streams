/**
 * A Stream Based Model that yilds when something changes
 * 
 * Stream1, Stream2, Stream3
 */
loop(stepper, seed, stream);

const model = () => {
    loop(function updateState(state,value){},{},)     
}